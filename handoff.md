# AgentLife 设计交接

## 当前阶段状态

项目按“一次只完成一个子系统”的方式进行产品级详细设计。本阶段已完成 **记忆系统（Memory）** 的第一版详细设计，并把 Working Memory、Recent Memory、Long-term Memory、主观实体档案和混合记忆检索纳入统一运行模型。

当前已完成：

* `docs/configuration-rule-infrastructure.md`
* `docs/world-system.md`
* `docs/character-system.md`
* `docs/body-system.md`
* `docs/perception-system.md`
* `docs/cognition-system.md`
* `docs/memory-system.md`

总设计及详细设计索引位于 `Readme.md`。具体语义以各详细设计文档为准，本交接只记录阶段结论和后续约束。

## 本阶段完成内容

新增 `docs/memory-system.md`，覆盖：

* Working、Recent 和 Long-term Memory 的职责、生命周期和删除语义。
* Working Memory 的确定性容量、离散负荷、准入、替换、快速衰减和真实删除。
* 从认知实际使用内容选择性编码 Recent Memory，以及 Recent 到 Long-term 的 Agent 巩固流程。
* 超窗 Recent 和巩固来源的运行时删除、受保护审计归档及严格用途隔离。
* 观察者私有的主观实体档案、带来源断言、冲突认识和来源失效。
* 不透明实体锚点、感知分辨率、确定性身份识别置信度及最小档案查询。
* Retrieval Intent、结构索引、关联图、必需但可替换的语义相似度、线索激活和受限 Agent 重排。
* 相关性与可达性分离、Working Memory 最终准入、消费确认和强化。
* 语义提供器版本、索引重建、保存恢复、失败阻塞及产品级概念接口。

同步修改：

* `Readme.md`：更新记忆总览、认知交互流程并加入详细设计索引。
* `docs/perception-system.md`：补充感知分辨率、身份识别置信度、伪装修正和最小档案查询。
* `docs/cognition-system.md`：补充 Working Memory 准入、Memory Skill、Retrieval Intent、消费确认和记忆候选交接。
* `docs/character-system.md`：明确主观实体档案属于观察者记忆，不属于被观察实体 Identity。
* `docs/configuration-rule-infrastructure.md`：补充记忆容量、相关性、可达性、识别置信度和语义版本的确定性配置边界。
* `handoff.md`：更新为当前阶段交接状态。

## 本阶段核心决策

### 统一记忆生命周期

运行时主链为：

**结构化观察与认知产物 → Working Memory → Recent Memory → Long-term Memory**

所有新观察、认知产物和检索结果都必须先通过 Working Memory。认知没有实际容纳和处理的信息不能绕过该边界直接形成持久记忆。

### Working Memory 是有限短时状态

Working Memory 维护结构化认知块，不等同于完整 LLM Context，也不复制认知系统维护的注意、疑问、意图和持续性决定。

认知块默认占用一个容量单位，配置可以按明确类型设置离散权重。总容量、身体或认知修正、替换优先级和快速衰减由确定性规则求值，不能依据自由文本长度或 LLM 自报难度计算。

未使用且未编码的内容到期后真实删除。已编码或检索出的内容离开 Working Memory 时只删除激活引用，不删除对应持久痕迹。

### Recent Memory 使用时间窗口而非容量

Recent Memory 不设置容量上限。认知 Agent 只能从实际使用的 Working Memory 内容中提交编码候选，记忆系统验证后建立痕迹和索引。

超出近期时间窗口仍未巩固的痕迹从运行时永久删除。它们不能再检索、激活或影响实体档案；保留的数据只进入受保护审计归档。

### Long-term Memory 由巩固形成

单条重要 Recent 痕迹可以迁移为 Long-term 状态；多条 Recent 痕迹可以由独立记忆 Agent 归纳为新的长期结果。长期结果提交成功后，相应来源从运行时 Recent 层删除，避免双份容量和检索噪声。

巩固允许概括、偏差和信息丢失，不保证恢复全部来源细节。巩固形成的习惯或偏好模式不能直接修改人物 Identity。

### 主观实体档案不是世界事实

每个 AI 角色的记忆实例分别维护对人物、物品、地点及其他可识别实体的主观档案。档案中的名称、类别、属性、印象和关系可以错误、冲突或过时。

档案是由仍存在的 Recent 或 Long-term 痕迹支持的物化索引视图，不是第四种记忆层。新断言必须由认知 Agent 明确提交；听见自我介绍不等于自动相信并写入档案。

支持来源失效时，没有其他支持的档案断言同步失效；巩固成功后可以改由长期结果支持。

### 实体识别不能利用 ID 作弊

世界实体 ID 只是不透明内部锚点，不是名称，也不贡献身份识别置信度。

感知系统先依据光线、距离、遮挡、感官效率、连续观察和伪装修正计算分辨率。达到可尝试识别的分辨率后，记忆系统再结合识别关联强度和可达性确定性计算身份置信度。

低于阈值时只返回当前分辨率允许的模糊描述；达到阈值时只返回观察者本地档案引用、首选称呼、数值置信度和自然语言等级。完整背景必须通过 Memory Skill 按需检索。

### 相关性和可达性分离

可达性表示一条记忆当前有多容易被想起，不能替代查询相关性。

相关性由结构索引、记忆关联图、语义相似度和当前情境共同形成。语义相似度是第一阶段必需能力，但不绑定具体嵌入模型、向量数据库或存储技术。

每个候选的相关性、基础可达性和抑制项共同形成综合激活。只有超过召回阈值的有限候选才能交给记忆 Agent 进行受限重排。

### Agent 不能查看完整记忆库

认知 Agent 通过 Memory Skill 在明确预算内提交和调整 Retrieval Intent。记忆 Agent 只能查看已经通过角色归属、状态、相关性、可达性和召回门槛的有限候选。

重排结果仍需通过 Working Memory 容量准入。命中不等于想起，只有认知实际使用并完成提交的结果才能得到强化。

### 审计归档不是休眠记忆

审计归档保存超窗删除的 Recent、巩固后移除的来源及失效档案断言，只服务授权调试和一致性审计。

归档不参与结构索引、关联图、语义索引、实体识别、可达性、巩固或提示构建。运行时组件和 Agent 永远不能把归档重新作为角色记忆读取。

### 语义能力必须版本化

语义提供器和表示版本属于运行配置。一次检索不能混合不兼容表示；更换模型时必须显式重建索引或迁移。

索引重建不能修改原始记忆内容、来源、可达性、关联或实体档案。语义提供器失败不能静默解释为角色自然遗忘。

### 记忆处理采用顺序稳定边界

记忆查询、重排、Working Memory 准入、认知消费、编码、档案更新、巩固、衰减、运行时删除和归档在固定稳定边界顺序完成。

不允许世界一边推进、记忆一边异步处理后再合并。任何必要记忆步骤失败时，当前模拟边界保持阻塞；有限重试失败后进入可见错误状态。

## 继承的整体系统约束

后续设计继续遵守：

* 模拟使用离散轮次和全局认知屏障；模型和用户等待的现实时间不计入模拟时间。
* 世界 Ground Truth、角色感知、Working Memory、持久记忆、认知理解和审计信息彼此分离。
* AI 输出和用户输入都必须通过身体执行和世界裁定才能影响客观事实。
* 用户与 AI 控制来源属于受保护系统信息，不是世界内可感知属性。
* 身体可以限制认知参与，但恢复信息必须经过感知过滤。
* 原始模型响应和调试追踪不属于角色认知或记忆。
* 用户角色可以没有完整 AI 认知和记忆实例，但不能因此获得信息或行动特权。
* 运行时只暴露一个原子生效的运行配置版本。

## 后续子系统必须继承的接入边界

* **时间与整体调度：** 独占模拟时钟，正式定义包含记忆阶段的稳定边界、全局认知屏障、参与者完成确认、保存边界和失败恢复语义。
* **配置生成 Skill：** 只能生成候选配置，必须通过共享验证、领域验证和原子配置更新；需要覆盖记忆容量、时间窗口、语义提供器引用、相关性组合和识别规则，但不能预写具体角色的隐藏记忆或实体认识。

## 尚待详细设计

* 时间与整体调度。
* 配置生成 Skill；轮到该子系统时需同时交付可供 AI Agent 使用的实际 Skill。

下一轮具体子系统由用户确认，并继续保持一次只详细设计一个子系统。

## 后续工作方式

1. 先阅读 `Readme.md`、本交接和 `docs/memory-system.md`。
2. 根据本轮目标读取直接相邻的详细设计；涉及跨系统权威边界时再检查所有受影响文档。
3. 先确认会改变产品边界的关键取舍，再写正式设计。
4. 保持世界观无关，不加入具体角色、地点、物品、剧情或平衡数值。
5. 不提前固定编程语言、存储技术、类结构或协议字段。
6. 新详细设计保存在 `docs` 并加入 `Readme.md` 索引。

## 工作区状态

本轮新增 `docs/memory-system.md`，同步修改 `Readme.md`、`handoff.md`、`docs/perception-system.md`、`docs/cognition-system.md`、`docs/character-system.md` 和 `docs/configuration-rule-infrastructure.md`。当前没有 Git 提交要求。
