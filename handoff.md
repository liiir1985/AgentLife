# AgentLife 设计交接

## 当前目标

项目正在逐个细化子系统设计。每次只处理一个子系统，输出产品级设计，不提前固定编程语言、存储方案、类结构或具体接口字段。

当前已完成世界系统的第一版详细设计。下一个按总文档顺序需要细化的子系统是 **角色系统（Character）**。

## 开始前阅读

1. `Readme.md`：项目目标、总体架构及各子系统的大框架。
2. `docs/world-system.md`：已经确定的世界系统职责和运行原则。

后续每个子系统使用独立文档，保存在 `docs` 目录，并在 `Readme.md` 的“子系统详细设计”中增加链接。

## 已确定的世界系统设计

* 世界状态是客观事实的唯一权威来源。
* 世界规则使用受限的声明式格式，不为每种物体或每个动作编写专用代码。
* 规则加载时编译并建立索引；Tick 只处理实际变化、到期过程和被触发的规则。
* Tick 只执行确定性规则和已经确定的一次性状态变更，不在 Tick 内调用 LLM。
* 遇到没有规则的新交互时，LLM 可以在 Tick 之外生成：
  * 可复用的受限规则；或
  * 只适用于当前情况的一次性状态变更。
* LLM 输出必须经过格式与一致性验证，不能直接修改世界。
* 当前事实模型无法表达、违反世界不变量或安全限制的结果应被拒绝。
* 规则生成后的执行是确定性的；世界运行成本应随实际变化和实际触发规则增长，而不是随全部实体、规则或 Tick 数增长。

## 新增但尚未细化的子系统

总文档已经加入 **配置生成 Skill（Configuration Authoring Skill）**。

它是设计时工具，不参与运行时模拟。未来细化该子系统时，需要同时交付一份可供 AI Agent 使用的 Skill，用于读取世界观、角色设计文档及配置规范，并生成世界事实、初始状态、角色配置、确定性规则及其他系统配置。

现在不要创建这个 Skill；等该子系统轮到详细设计时再创建。

## 下一步：角色系统详细设计

建议创建 `docs/character-system.md`，并在 `Readme.md` 中加入链接。

本轮只设计角色系统自身，重点需要澄清：

* 角色实例由哪些稳定信息和运行时状态构成。
* Identity、Body、Perception、Cognition、Memory、Continuity 在角色系统中的归属关系。
* 角色系统是聚合边界还是包含其他子系统的实现。
* 用户角色与 AI 角色共享哪些结构，差异由什么表达。
* 角色创建、加载、运行、暂停及生命周期结束的产品语义。
* 角色配置与角色运行状态如何区分。

不要在角色系统文档中展开身体、感知、认知或记忆的内部算法；只定义角色系统如何组织它们以及系统之间的责任边界。

具体世界观、具体角色身份、人格内容和背景故事不属于本轮目标。

## 协作偏好

用户偏好简短、直接的说明。先用最简单的语言说明核心设计，再按需展开。

避免：

* 一次给出大量边界条件和案例。
* 把其他子系统的运行细节混入当前子系统。
* 在用户尚未确认方向时创建额外交付物。
* 用并行化解释系统可扩展性；这里关注的是总工作量和增量执行。

如果存在关键产品取舍，先提出最少量的问题确认，再写正式设计。

## 范围与文件状态

* 当前详细设计保持世界观无关，不包含具体地点、物品、角色内容或验收场景。
* `Readme.md` 和 `docs/` 当前是未跟踪文件。
* `.gitattributes` 当前处于删除状态，这是已有状态，不要恢复或修改。
* 当前没有 Git 提交要求。
