# AgentLife 设计交接

## 当前阶段状态

项目按“一次只完成一个子系统”的方式进行产品级详细设计。本阶段已完成 **认知系统与认知调度（Cognition and Cognitive Scheduling）** 的第一版详细设计，并根据讨论将运行模型确立为“离散模拟轮次与全局认知屏障”。

当前已完成：

* `docs/configuration-rule-infrastructure.md`
* `docs/world-system.md`
* `docs/character-system.md`
* `docs/body-system.md`
* `docs/perception-system.md`
* `docs/cognition-system.md`

总设计及详细设计索引位于 `Readme.md`。具体语义以各详细设计文档为准，本交接只记录阶段结论和后续约束。

## 本阶段完成内容

新增并修订 `docs/cognition-system.md`，覆盖：

* 离散模拟轮次、稳定决策边界和全局认知屏障。
* 多角色现实计算并发与模拟时间并行推进之间的严格区别。
* AI 角色一次认知调用同时形成最小认知更新、表达和身体计划。
* 用户角色等待输入、输入解释 LLM、明确跳过和澄清流程。
* AI 与用户计划的统一身体语义及相同世界规则。
* 自然语言认知内容、Identity、记忆和调试记录的分层。
* 动作期间反应机会、认知中断与恢复、保存和失败处理。
* 产品级概念接口和验收场景。

同步修改：

* `Readme.md`：更新认知、认知调度及统一时间模型。
* `docs/character-system.md`：补充用户替代认知来源、等待输入、跳过和输入解释路径。
* `docs/world-system.md`：明确认知屏障期间世界过程不推进。
* `docs/body-system.md`：明确认知屏障期间身体保持已提交状态。
* `docs/perception-system.md`：把建议唤醒接入当前决策轮次和屏障。
* `docs/configuration-rule-infrastructure.md`：更新认知调度和整体时间接入边界。
* `handoff.md`：更新为当前阶段交接状态。

## 本阶段核心决策

### 离散模拟而非实时模拟

LLM 调用通常需要远高于一个世界 Tick 的现实时间，因此系统不尝试在等待模型时实时推进世界。模型调用和用户等待消耗的现实时间不计入模拟时间。

一个完整模拟轮次由确定性世界与身体更新、感知形成、认知需求判断、必要的认知屏障、决定提交和身体计划接纳组成。世界 Tick 和身体 Tick 自身仍只执行确定性逻辑，但如果其结果产生认知需求，下一个 Tick 必须等待当前认知阶段完成。

### 全局认知屏障

稳定时间点出现需要理解、回应或选择的事件时，认知调度确定本轮所有必要 AI 与用户参与者并建立全局屏障：

* 模拟时间停止推进。
* 世界过程和身体动作保持当前已提交进度。
* 所有参与者读取同一模拟时间点下各自允许的信息。
* 所有必要决定完成、跳过或被明确处置后，才能进入下一轮。

屏障是系统调度状态，不是世界内事实。角色不会感知模型或用户在现实中等待了多久。

### 多角色并发边界

多个 AI 模型调用可以为了缩短墙上时间并发执行，但这只是计算层并发，不是模拟时间并行。较早返回的模型结果不能先执行，也不能被其他本轮参与者看到。

本轮所有决定收集完成后，身体计划统一交给身体系统。模型响应顺序不能形成模拟先手优势。

### AI 角色决定

AI 角色的一次认知 LLM 调用同时产生：

* 下一轮需要延续的最小认知状态更新。
* 本轮表达内容。
* 一个短身体计划，或明确不采取新动作。
* 必要的持续性决定、记忆材料或 Identity 变化提议。

不固定增加第二次 LLM Pass 将认知转换为动作。模型结果只进行确定性的格式、来源、权限和身体计划语义验证。

### 用户角色决定

用户角色需要判断时，用户本人替代自主认知 LLM。全局屏障等待用户：

* 提交自然语言意图。
* 明确跳过当前决策轮次。
* 取消或保存当前模拟。

用户自然语言由输入解释 LLM 转换为表达和结构化身体计划。解释器不能为用户补造没有表达的长期目标或自主行为；输入含糊时保持当前轮次并请求澄清。

用户跳过表示本轮不产生新表达或动作，不会取消已有动作、删除持续事项或使角色退出世界。用户与 AI 计划接受相同身体能力、资源、阶段和世界裁定规则。

### 认知内容并非只有机械结构

当前认知状态只保存下一次认知需要延续的最小内容：注意、主观理解、疑问、当前意图和持续性决定。

其中的理解、疑问和理由可以使用丰富自然语言。结构只明确内容类型、来源、状态和消费者，不把角色思想压缩成枚举或数值。

稳定人格、偏好和自我认识属于人物 Identity；长期经历、重要反思和习惯形成属于记忆系统。单次认知可以把重要理解和决定理由交给记忆系统，但原始模型响应和供应商隐藏推理不属于角色记忆。

### 动作与反应机会

没有新认知需求时，跨 Tick 动作和世界过程继续确定性推进。新对话、高显著刺激、动作异常或明确决策点在合法稳定边界建立屏障，使角色在后续推进前获得反应机会。

认知不能倒改当前轮次已经提交的瞬时结果。需要允许反应的攻击或其他行为，应由世界和身体规则定义可产生感知与决定机会的阶段。

### 失败与保存

AI 调用失败、用户尚未输入或输入需要澄清时，屏障保持，世界不能绕过未解决决定继续推进。自动重试达到限制后进入可见失败状态，由用户选择重试、调整、保存或终止。

屏障期间可以保存。恢复后仍处于同一模拟时间点，并保留参与者完成状态；进行中的外部请求可以重新发起，旧请求迟到响应不能重复提交。迟到防护只服务取消、重试和保存恢复，不用于支持世界在等待 LLM 时继续运行。

## 必须继承的既有基础设施决策

### 配置和规则复用

所有子系统必须复用统一配置能力；只有能够确定性声明的逻辑才按需使用共享规则内核。共享基础设施只产生候选条件或效果，不能直接修改领域权威状态。

认知需求条件可以使用规则内核，但参与者集合、全局屏障、AI 调用、用户等待、失败处理和轮次完成由认知调度负责。任何规则都不能在必要参与者完成前推进模拟时间。

### 单一运行配置版本与原子更新

运行时只暴露一个运行配置版本。配置输入、解析、共享验证、领域验证、编译和生效属于原子操作；失败时当前配置不变，成功后一次提交完整版本。

配置更新不能倒改已经提交的世界、身体、感知或认知状态。

### 信息和权威边界

后续设计继续遵守：

* 世界 Ground Truth、角色感知、认知理解和记忆相互分离。
* AI 输出和用户输入都必须通过身体执行和世界裁定才能影响客观事实。
* 用户与 AI 控制来源属于受保护系统信息，不是世界内可感知属性。
* 身体可以限制认知参与，但恢复信息必须经过感知过滤。
* 原始模型响应和调试追踪不属于角色认知或记忆。
* 用户角色可以没有完整 AI 认知和记忆实例，但其可用信息和行动规则不能因此获得特权。
* 配置与运行状态严格分离。

## 后续子系统必须继承的接入边界

* **记忆系统：** 从 AI 角色实际感知并提交的主观理解、反思和决定中选择性形成记忆；支持习惯与长期变化所需的经历模式，但不保存原始模型响应作为角色记忆。
* **时间与整体调度：** 独占模拟时钟，正式定义模拟轮次、稳定阶段、全局认知屏障、参与者完成确认、保存边界和恢复语义。
* **配置生成 Skill：** 只能生成候选配置，必须通过共享验证、领域验证和原子配置更新；不能把具体角色思想写成隐藏强制规则。本阶段不创建该 Skill。

## 尚待详细设计

* 记忆系统。
* 时间与整体调度。
* 配置生成 Skill；轮到该子系统时需同时交付可供 AI Agent 使用的实际 Skill。

下一轮具体子系统由用户确认，并继续保持一次只详细设计一个子系统。

## 后续工作方式

1. 先阅读 `Readme.md` 和本交接。
2. 根据本轮目标读取直接相邻的详细设计；涉及跨系统权威边界时再检查所有受影响文档。
3. 先确认会改变产品边界的关键取舍，再写正式设计。
4. 保持世界观无关，不加入具体角色、地点、物品、剧情或平衡数值。
5. 不提前固定编程语言、存储技术、类结构或协议字段。
6. 新详细设计保存在 `docs` 并加入 `Readme.md` 索引。

## 工作区状态

本轮新增并修订 `docs/cognition-system.md`，同步修改 `Readme.md`、`handoff.md` 及五份相邻子系统文档。继续工作时保留这些设计，不要覆盖、删除或回退用户已有修改。当前没有 Git 提交要求。
