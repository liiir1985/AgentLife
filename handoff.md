# AgentLife 设计交接

## 当前阶段状态

项目按“一次只完成一个子系统”的方式进行产品级详细设计。当前已完成配置生成 Skill 第一版，并同步统一配置基础设施、人物、身体、感知、认知和记忆的接入语义。最新设计明确 Skill 使用语义确定的结构化字段接收自然语言文本，只输出首版候选配置或初始记忆候选，不负责持久化、审阅或应用。

当前已完成：

* `docs/configuration-rule-infrastructure.md`
* `docs/world-system.md`
* `docs/character-system.md`
* `docs/body-system.md`
* `docs/perception-system.md`
* `docs/cognition-system.md`
* `docs/memory-system.md`
* `docs/time-orchestration-system.md`
* `docs/configuration-authoring-skill.md`

总设计及详细设计索引位于 `Readme.md`。具体语义以各详细设计文档为准，本交接只记录阶段结论和后续约束。

实现层面已按 `docs/implementation-roadmap.md` 完成**阶段 0：技术 Spike**、**阶段 1：内容、配置与规则基础**、**阶段 2：确定性模拟内核**、**阶段 3：可玩 TUI 竖切**、**阶段 4：感知与 Pi 认知**和**阶段 5：主观记忆闭环**。阶段 5 包括 Working → Recent → Long-term、独立主观档案、受限检索、身份回接、存档版本 4 及玩家记忆入口。各阶段证据与暂缓范围见 `docs/phase-0-spike-report.md` 至 `docs/phase-5-report.md`；阶段 6 的 Fast Forward 调度尚未实施。

阶段 1 交付的运行时能力集中在 `src/config`（身份/诊断/数值与映射/取值与条件/扩展注册/值族与词汇展开/内容包解析/解析与来源/七阶段验证/编译/确定性求值/原子更新/演示运行器）与 `src/storage`（迁移、`RuntimeStore`、配置提交崩溃注入），领域词汇以扩展形式注册在 `src/domains`（world/body/character），演示内容为仓库内的 `content/demo`。规则只产生候选效果并返回完整追踪；领域提交、动作生命周期、过程推进与 Tick 阶段仍属阶段 2。进入阶段 2 前先读 `docs/phase-1-report.md` 的 §13 硬约束，以及 §15 记录的阶段 1 偏差（**值族取代硬编码指标词汇**：值由内容定义展开为 `agentlife.body/values.<id>`、`agentlife.body/channels.<id>.*`、`agentlife.world/environment.<id>`；契约值单写方；组合方式由规则声明）。

## 已有配置生成 Skill 设计内容

新增 `docs/configuration-authoring-skill.md`，覆盖：

* 输入和输出均为结构化数据，但输入字段可以保存完整自然语言文本。
* 字段类型、归属、用途和允许输出先于 Skill 确定；上游不必预先拆成最终配置项或记忆痕迹。
* 初始配置生成与初始记忆提取是两个明确模式，输出和下游验证路径严格分离。
* Skill 只支持首版候选生成，不读取当前有效配置作为增量基线。
* 缺失、规范默认值请求、未确定、冲突和不适用具有不同语义。
* 候选配置保留规范版本、引用、依赖、输入字段及文本片段来源。
* 初始记忆只来自明确合格的角色背景经历、对话或世界内记录字段。
* 初始记忆候选只包含 Long-term Memory 及由其支持的实体档案断言，不伪造 Working、Recent 或巩固事件。
* Skill 可以消费外部验证诊断生成修订候选，但不拥有权威验证、持久化或提交权限。
* 候选持久化、用户审阅、批准、验证编排和应用属于独立后续系统，当前不设计用户界面。
* 行为树采用第三方组件，AgentLife 只保留确定性、零 LLM、受限视图、有限黑板和统一身体计划输出的适配契约。

同步修改：

* `Readme.md`：更新配置生成 Skill 总览、验收项和详细设计索引。
* `docs/configuration-rule-infrastructure.md`：明确候选生成、权威验证和原子应用的责任分离。
* `docs/character-system.md`：明确自然语言 Identity 字段、合格记忆来源字段和人物创建之间的边界。
* `docs/memory-system.md`：补充初始记忆候选验证与模拟前原子导入语义。
* `docs/body-system.md`、`docs/perception-system.md` 和 `docs/cognition-system.md`：同步结构化自然语言输入及只产出候选的协作语义。

## 已有时间与整体调度设计内容

新增 `docs/time-orchestration-system.md`，覆盖：

* 唯一模拟时钟、配置化固定 Tick、单步、普通连续、暂停和静止状态。
* Fast Forward 使用完整 Tick 高速推进，不采用事件跳时或近似模拟。
* Fast Forward 的系统级零 LLM 不变量，以及规则缺失时先退出快进再建立屏障。
* 普通动态、降级和正常实体形成递进能力层级，而不是并列的互斥对象模型。
* 降级实体是普通动态实体的超集，增加行为树、有限黑板及动作执行能力。
* 正常实体继续复用共同世界、身体和生命周期基础，但用完整感知、认知和记忆模块替换行为树决策模块。
* 主要实体标记是正常实体上的独立调度属性，不由控制来源或世界身份自动推出；普通动态和降级实体不具备主要资格。
* 非主要正常实体在快进中跳过认知、不阻塞、不排队且不补算经历。
* 睡眠、昏迷和麻醉由身体状态、恢复过程及外部刺激逐 Tick 决定，不使用固定期限自动唤醒。
* 规则解析屏障先于认知屏障，以及同 Tick 确定性传播、收敛限制和新计划下一 Tick 生效。
* 安全保存边界、阶段日志、幂等身份、迟到响应隔离和失败后保留并续做。
* 时间控制、主要实体、活动登记、行为树调度、屏障协调和授权诊断等产品级接口。

同步修改：

* `Readme.md`：加入固定 Tick、Fast Forward、主要实体和降级实体总览及详细设计索引。
* `docs/world-system.md`：改为固定 Tick 活动索引入口，并明确规则屏障发生在感知和认知之前。
* `docs/character-system.md`：加入递进能力层级、主要标记、决策模块替换和配置限制。
* `docs/body-system.md`：明确睡眠及昏迷逐 Tick 演化、行为树计划共用身体入口和新计划下一 Tick 生效。
* `docs/perception-system.md`：加入离屏观察折叠、不回放、离屏空档与身体中断区分，以及行为树局部视图边界。
* `docs/cognition-system.md`：加入主要标记的调度边界、Fast Forward 零 LLM、非主要认知跳过和降级实体不参与屏障。
* `docs/memory-system.md`：加入快进中的确定性维护、模型任务保护延后、禁止离屏记忆补写和降级实体无主观记忆。
* `docs/configuration-rule-infrastructure.md`：加入 Tick、实体能力层级、决策模块互斥、主要标记、行为树权限和零 LLM 策略的跨领域验证。

## 已有记忆设计内容

新增 `docs/memory-system.md`，覆盖：

* Working、Recent 和 Long-term Memory 的职责、生命周期和删除语义。
* Working Memory 的确定性容量、离散负荷、准入、替换、快速衰减和真实删除。
* 从认知实际使用内容选择性编码 Recent Memory，以及 Recent 到 Long-term 的 Agent 巩固流程。
* 超窗 Recent 和巩固来源的运行时删除、受保护审计归档及严格用途隔离。
* 观察者私有的主观实体档案、带来源断言、冲突认识和来源失效。
* 不透明实体锚点、感知分辨率、确定性身份识别置信度及最小档案查询。
* Retrieval Intent、结构索引、关联图、必需但可替换的语义相似度、线索激活和受限 Agent 重排。
* 相关性与可达性分离、Working Memory 最终准入、消费确认和强化。
* 语义提供器版本、索引重建、保存恢复、失败阻塞及产品级概念接口。

同步修改：

* `Readme.md`：更新记忆总览、认知交互流程并加入详细设计索引。
* `docs/perception-system.md`：补充感知分辨率、身份识别置信度、伪装修正和最小档案查询。
* `docs/cognition-system.md`：补充 Working Memory 准入、Memory Skill、Retrieval Intent、消费确认和记忆候选交接。
* `docs/character-system.md`：明确主观实体档案属于观察者记忆，不属于被观察实体 Identity。
* `docs/configuration-rule-infrastructure.md`：补充记忆容量、相关性、可达性、识别置信度和语义版本的确定性配置边界。
* `handoff.md`：更新为当前阶段交接状态。

## 此前认知与记忆补全内容

* `docs/cognition-system.md`：意图从单一“当前意图”扩展为认知系统拥有的多条前瞻性状态，补充形成来源、生命周期、情境召回、使用强化、衰减遗忘和空闲契约。
* `docs/memory-system.md`：为意图候选提供独立类型的检索与 Working Memory 预留容量接入，但不接管意图状态或竞争；明确记忆片段解释与行动决策处于同一次认知调用。
* `docs/character-system.md`：明确 Identity 人物历史是角色定义和认知背景，不会在人物创建时自动生成 Long-term Memory。
* `docs/configuration-rule-infrastructure.md`：补充意图相关性、可达性、衰减、容量裁剪、等待期限和硬上限的确定性规则边界。
* `docs/perception-system.md`：将旧的独立“主观回忆重建”表述改为认知决策调用内的记忆片段解释边界。
* `Readme.md`：同步自主意图、内联记忆解释、稳定交互流程和初始资料提取能力的位置。
* 统一认知与记忆稳定边界顺序，使观察、意图和记忆召回、单次认知、状态提交、巩固与衰减在模拟继续前完整结束。

## 本阶段核心决策

### 统一记忆生命周期

运行时主链为：

**结构化观察与认知产物 → Working Memory → Recent Memory → Long-term Memory**

所有新观察、认知产物和检索结果都必须先通过 Working Memory。认知没有实际容纳和处理的信息不能绕过该边界直接形成持久记忆。

### Working Memory 是有限短时状态

Working Memory 维护结构化认知块，不等同于完整 LLM Context，也不复制认知系统维护的注意、疑问、意图和持续性决定。相关意图只以指向认知权威状态的激活引用进入配置化预留容量。

认知块默认占用一个容量单位，配置可以按明确类型设置离散权重。总容量、身体或认知修正、替换优先级和快速衰减由确定性规则求值，不能依据自由文本长度或 LLM 自报难度计算。

未使用且未编码的内容到期后真实删除。已编码或检索出的内容离开 Working Memory 时只删除激活引用，不删除对应持久痕迹。

### Recent Memory 使用时间窗口而非容量

Recent Memory 不设置容量上限。认知 Agent 只能从实际使用的 Working Memory 内容中提交编码候选，记忆系统验证后建立痕迹和索引。

超出近期时间窗口仍未巩固的痕迹从运行时永久删除。它们不能再检索、激活或影响实体档案；保留的数据只进入受保护审计归档。

### Long-term Memory 由巩固形成

单条重要 Recent 痕迹可以迁移为 Long-term 状态；多条 Recent 痕迹可以由独立记忆 Agent 归纳为新的长期结果。长期结果提交成功后，相应来源从运行时 Recent 层删除，避免双份容量和检索噪声。

巩固允许概括、偏差和信息丢失，不保证恢复全部来源细节。巩固形成的习惯或偏好模式不能直接修改人物 Identity。

### 主观实体档案不是世界事实

每个 AI 角色的记忆实例分别维护对人物、物品、地点及其他可识别实体的主观档案。档案中的名称、类别、属性、印象和关系可以错误、冲突或过时。

档案是由仍存在的 Recent 或 Long-term 痕迹支持的物化索引视图，不是第四种记忆层。新断言必须由认知 Agent 明确提交；听见自我介绍不等于自动相信并写入档案。

支持来源失效时，没有其他支持的档案断言同步失效；巩固成功后可以改由长期结果支持。

### 实体识别不能利用 ID 作弊

世界实体 ID 只是不透明内部锚点，不是名称，也不贡献身份识别置信度。

感知系统先依据光线、距离、遮挡、感官效率、连续观察和伪装修正计算分辨率。达到可尝试识别的分辨率后，记忆系统再结合识别关联强度和可达性确定性计算身份置信度。

低于阈值时只返回当前分辨率允许的模糊描述；达到阈值时只返回观察者本地档案引用、首选称呼、数值置信度和自然语言等级。完整背景必须通过 Memory Skill 按需检索。

### 相关性和可达性分离

可达性表示一条记忆当前有多容易被想起，不能替代查询相关性。

相关性由结构索引、记忆关联图、语义相似度和当前情境共同形成。语义相似度是第一阶段必需能力，但不绑定具体嵌入模型、向量数据库或存储技术。

每个候选的相关性、基础可达性和抑制项共同形成综合激活。只有超过召回阈值的有限候选才能交给记忆 Agent 进行受限重排。

### Agent 不能查看完整记忆库

认知 Agent 通过 Memory Skill 在明确预算内提交和调整 Retrieval Intent。记忆 Agent 只能查看已经通过角色归属、状态、相关性、可达性和召回门槛的有限候选。

重排结果仍需通过 Working Memory 容量准入。命中不等于想起，只有认知实际使用并完成提交的结果才能得到强化。

### 意图是认知拥有的前瞻性状态

AI 角色可以同时保有多条未完成意图。意图不是必须执行的任务，也不是记忆痕迹；认知 Agent 根据 Identity、身体需要、观察、记忆、承诺和行动结果自主建立、暂停、满足或放弃意图。

系统只按当前情境召回相关意图，并通过 Working Memory 预留容量限制数量。召回排序不能宣布获胜意图；认知在同一次调用中看到相关意图后，自行决定行动可以服务一条、多条或不服务任何意图。

只有认知确认实际考虑或追随的意图才强化。长期未使用意图可以衰减并退出运行时；遗忘不会自动生成长期记忆，关于旧意图的新反思仍需正常编码。

### 空闲必须有有限唤醒边界

AI 没有提交立即行动时，必须等待可验证外部事件、登记确定性重审条件，或进入具有后续决策边界的持续活动。等待期限由规则求值并受全局硬上限约束；到期只触发重新认知，不强制行动或继续旧意图。

### 记忆解释不增加认知 Pass

记忆系统返回已经通过权限、相关性、可达性、重排和 Working Memory 准入的带来源片段。认知 Agent 在形成行动决定的同一次 LLM 调用中处理片段缺失、冲突和不确定性，不建立独立主观回忆重建 Agent 或持久对象。

临时解释不会自动成为新记忆。只有认知明确提交带消费来源的新反思、判断或关系认识，并通过编码验证后，才形成新的 Recent 痕迹。

### Identity 背景不等于初始记忆

人物小传和个人历史属于 Identity 及认知背景，不会在人物或记忆实例创建时自动拆成 Long-term Memory。配置生成 Skill 只有在初始记忆模式中接收到语义、目标角色、来源资格和用途明确的背景经历、对话或世界内记录字段时，才会从其中的自然语言生成带来源的初始记忆与实体档案候选。候选仍需记忆领域验证，并通过模拟开始前的专用导入边界生效。

### 审计归档不是休眠记忆

审计归档保存超窗删除的 Recent、巩固后移除的来源及失效档案断言，只服务授权调试和一致性审计。

归档不参与结构索引、关联图、语义索引、实体识别、可达性、巩固或提示构建。运行时组件和 Agent 永远不能把归档重新作为角色记忆读取。

### 语义能力必须版本化

语义提供器和表示版本属于运行配置。一次检索不能混合不兼容表示；更换模型时必须显式重建索引或迁移。

索引重建不能修改原始记忆内容、来源、可达性、关联或实体档案。语义提供器失败不能静默解释为角色自然遗忘。

### 记忆处理采用顺序稳定边界

记忆查询、重排、Working Memory 准入、认知消费、编码、档案更新、巩固、衰减、运行时删除和归档在固定稳定边界顺序完成。

不允许世界一边推进、记忆一边异步处理后再合并。任何必要记忆步骤失败时，当前模拟边界保持阻塞；有限重试失败后进入可见错误状态。

## 继承的整体系统约束

后续设计继续遵守：

* 模拟使用离散轮次和全局认知屏障；模型和用户等待的现实时间不计入模拟时间。
* 世界 Ground Truth、角色感知、Working Memory、持久记忆、认知理解和审计信息彼此分离。
* AI 输出和用户输入都必须通过身体执行和世界裁定才能影响客观事实。
* 用户与 AI 控制来源属于受保护系统信息，不是世界内可感知属性。
* 身体可以限制认知参与，但恢复信息必须经过感知过滤。
* 原始模型响应和调试追踪不属于角色认知或记忆。
* 用户角色属于正常实体，拥有完整感知、认知连续状态和主观记忆，但由用户决定入口替代自主认知 LLM。
* 运行时只暴露一个原子生效的运行配置版本。
* 单步、普通连续和 Fast Forward 使用相同固定 Tick；快进不能跳过领域阶段。
* Fast Forward 期间系统级禁止全部 LLM，不存在非主要角色认知、记忆 Agent 或规则生成例外。
* 普通动态实体提供基础运行能力；降级实体增加行为树；正常实体以完整主观链路替换行为树，不能同时运行两套决策器。
* 主要实体标记与正常能力层级分别配置，不属于世界事实或角色可见属性。
* 非主要正常实体在快进期间牺牲主观连续性，不积累或补算经历。
* 普通动态和降级实体必须非主要；降级实体只能使用确定性行为树、有限黑板和受限局部执行视图。
* 睡眠、昏迷和认知恢复由身体状态逐 Tick 演化，调度器不按名称或固定期限唤醒。

## 后续系统必须继承的接入边界

* **配置资料输入：** 自然语言可以直接保存在结构化字段中，但字段类型、归属、用途和允许输出必须唯一确定；不能把未分类纯文本交给 Skill 自行判断用途。
* **配置生成 Skill：** 只生成首版候选配置或初始记忆候选，不能持久化、审阅、批准、应用或修改运行时状态。
* **配置审阅及应用系统：** 负责候选持久化、用户审阅、批准、权威验证编排、原子应用、版本管理和失败恢复；具体用户界面留待该系统设计时确定。
* **初始记忆导入：** 只能接收带角色、视角、认识方式和文本片段来源的候选，在模拟开始前通过记忆系统专用验证与原子导入边界处理。
* **第三方行为树适配：** 必须保持确定性、无 LLM、受限局部输入和普通身体计划输出，不得把黑板扩展成隐藏认知或主观记忆系统。

## 后续可能的独立系统

* 结构化设计资料的创建、编辑或导入工具；只需保证自然语言字段具有明确语义，不要求预先拆成高级领域数据。
* 配置候选的持久化、用户审阅、批准和应用系统；用户界面与呈现方式在该系统设计时确定。
* 配置生成 Skill 的实际平台实现；当前详细设计保持平台、模型和协议无关。
* 初始记忆导入的工程实现；产品语义已经在记忆系统中确定。

第三方行为树的节点语言、编辑器、行为内容和调试能力直接使用满足适配契约的外部组件，不再作为 AgentLife 的独立详细设计子系统。

## 后续工作方式

1. 先阅读 `Readme.md`、本交接和当前目标的详细设计；涉及配置创作时重点阅读 `docs/configuration-authoring-skill.md`，涉及主观连续性时重点阅读 `docs/memory-system.md`。
2. 根据本轮目标读取直接相邻的详细设计；涉及时间、快进、主要标记、行为树或跨系统权威边界时同时检查 `docs/time-orchestration-system.md`。
3. 先确认会改变产品边界的关键取舍，再写正式设计。
4. 保持世界观无关，不加入具体角色、地点、物品、剧情或平衡数值。
5. 不提前固定编程语言、存储技术、类结构或协议字段。
6. 新详细设计保存在 `docs` 并加入 `Readme.md` 索引。

## 工作区状态

设计阶段新增 `docs/configuration-authoring-skill.md`，并同步修改 `Readme.md`、`handoff.md`、`docs/configuration-rule-infrastructure.md`、`docs/character-system.md`、`docs/body-system.md`、`docs/perception-system.md`、`docs/cognition-system.md` 和 `docs/memory-system.md`。未实现实际 Skill、结构化资料编辑工具或配置审阅及应用系统；第三方行为树内部设计不属于 AgentLife。

随后按 `docs/implementation-roadmap.md` 的**阶段 0：技术 Spike** 建立了工程入口（Node 24 + ESM + pnpm + Vitest + 严格 TS，依赖精确锁定并提交 lockfile），五组 Spike 的适配器与测试位于 `src/{agent,content,storage,behavior,tui}` 与 `tests`，结论、风险和阶段 1 硬约束记录在 `docs/phase-0-spike-report.md`。阶段 0 已判定完成（`pnpm phase0:verify` 全绿，Windows Terminal 人工清单全部通过）：Mistreevous **受限接受**，Drizzle node-sqlite RC 线接受，TUI 保留 Pi TUI、不启用 Web 回退。

**阶段 1：内容、配置与规则基础**已完成（`pnpm phase1:verify` 全绿，11 个测试文件 159 passed | 1 skipped；`pnpm config:demo` 两次运行摘要一致）：内容包规范与最小演示内容 `content/demo`、七阶段验证与结构化诊断、不可变运行配置与触发索引、值族词汇展开（内容定义 → 视图成员与可写目标）、硬阈值/分段常量/分段线性映射与优先级/最小/最大/加法/乘法组合、取值来源的组合/比较/选择、原子配置更新与崩溃恢复、SQLite 迁移与版本化载荷、恢复身份核对。结论、验收场景对照、硬约束、暂缓范围与本阶段偏差见 `docs/phase-1-report.md`。

**阶段 2：确定性模拟内核**与**阶段 3：可玩 TUI 竖切**分别完成 Tick 阶段与动作生命周期、行为树计划、世界裁定、稳定边界保存与加载续做，以及固定区块界面、地点/人物/物品/结果渲染、动作菜单与参数向导、`/step`、`/run`、`/pause`、`/save`、`/load`、`/status`、`/monitor` 与普通/管理视图的信息隔离；结论见 `docs/phase-2-report.md` 与 `docs/phase-3-report.md`。

**阶段 4：感知与 Pi 认知**已完成（`pnpm phase4:verify` 全绿，25 个测试文件 247 passed | 1 skipped；`pnpm phase4:demo` 两次运行的原时间线继续与加载后继续摘要一致）：感知可达性与条件、分辨率等级与内容化投影、出现/持续/变化/消失/重现/事件/自身结果观察、观察者局部引用与重复抑制、说话动作完成后才产生可听见事件、授权玩家视图与动作候选筛选、最小 Working Memory（准入、容量裁剪、确认释放）、认知状态与多条意图、空闲契约、认知需求与全局屏障（等待模型与玩家期间冻结 Tick）、同轮计划稳定排序的统一交接、Pi 认知 Agent（严格原始参数校验、请求身份、迟到响应隔离、重试与失败停止）、屏障保存与存档版本 3（版本 2 拒绝）。结论、验收对照、已知限制与偏差见 `docs/phase-4-report.md`；身份关联、Recent/Long-term 记忆与巩固属阶段 5，进入前先读该报告的 §4 已知限制与 `docs/memory-system.md`。

**阶段 5：主观记忆闭环**已完成：记忆配置与状态契约、Working 衰减和来源核验、Recent 编码及删除、Memory Agent 巩固为 Long-term、角色私有档案、有限混合检索、感知身份关联、玩家 `M` 记忆入口、存档版本 4。Embedding 提供器、模型、地址和表示版本位于根目录 `config/agentlife.yaml`，不属于 content 包；HTTP 服务由用户安装和运行。详细交付和验证限制见 `docs/phase-5-report.md`。阶段 6 仍需实现 Fast Forward 完整调度。
