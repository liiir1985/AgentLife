# AgentLife 设计交接

## 当前阶段状态

项目按“一次只完成一个子系统”的方式进行产品级详细设计。本阶段已完成 **感知系统（Perception）** 的第一版详细设计，并确定人物识别所需的不透明实体锚点与主观身份关联边界。

当前已完成：

* `docs/configuration-rule-infrastructure.md`
* `docs/world-system.md`
* `docs/character-system.md`
* `docs/body-system.md`
* `docs/perception-system.md`

总设计及详细设计索引位于 `Readme.md`。具体语义以各详细设计文档为准，本交接只记录阶段结论和后续约束。

## 本阶段完成内容

新增 `docs/perception-system.md`，覆盖：

* 客观感知材料、候选刺激、结构化观察、待处理感知流、恢复上下文和诊断信息分层。
* 感官可达性、注意、显著度、细节分辨率及确定性感知处理流程。
* 不透明实体锚点、观察连续性和基于主观身份关联的轻量人物识别。
* 感知流消费、连续变化、聚合、重复抑制及建议认知唤醒。
* 清醒但未调用 LLM 与明确认知中断的区别，以及受限恢复上下文。
* 保存恢复、版本一致、跨系统接口、失败语义和验收场景。

同步修改：

* `Readme.md`：加入感知系统详细设计链接。
* `handoff.md`：更新为当前阶段交接状态。

## 本阶段感知系统决策

### 结构化有限观察

感知系统的权威结果是结构化观察，表达角色实际获得的外在对象、属性、动作、空间来源、细节程度和不确定性。自然语言只作为认知输入的稳定渲染，不与结构化观察并列成为权威状态，也不能增加因果、意图或隐藏身份。

感知运行时不调用 LLM。系统可以把客观材料组织为“门口有一个人形轮廓”这类日常观察，但“对方正在偷听”或“故意叫醒我”等解释留给认知。

实体事实通过配置化的实体感知投影变成结构化描述。世界感知材料视图只提供投影白名单允许引用的外在事实；统一规则基础设施依据通道、分辨率和映射生成候选描述；感知系统验证并提交最终观察。低分辨率使用明确的粗化描述或省略细节，任何阶段都不能把完整实体状态通用序列化后再尝试删除隐藏字段。

### 不透明实体锚点与人物识别

世界材料可以携带来源实体的唯一标识，但感知只把它作为不透明锚点，用于连续追踪、去重和查询当前观察者已有的主观身份关联。认知不能用该锚点读取人物设定、真实身份、关系、认知或记忆。

第一阶段不模拟脸部、声纹或步态特征匹配。人物识别采用“当前条件足以辨认”与“角色记忆中已有该实体的主观身份关联”两个条件：

* 可辨认且已有主观关联时，可以认出自己所认识的对象。
* 可辨认但没有关联时，只能得到一个不认识的人。
* 当前条件不足时，即使存在关联也不能直接显示身份。

第一次见面不会读取对方完整设定。自我介绍只是一次感知到的表达，是否相信及是否建立身份关联由后续认知与记忆流程决定。主观身份可以缺失、错误或过时，世界真实身份不会自动纠正角色认识。

### 注意、感知流与认知调度

认知系统提供当前注意，感知优先处理相关材料；高显著刺激可以突破注意并形成建议唤醒。规则和感知系统都不能直接调用 LLM，实际排队、去重和唤醒由认知调度器负责。

结构化观察可以在清醒角色没有运行 LLM 时进入待处理感知流。稳定材料通过连续追踪、变化检测、聚合和重复抑制避免每个 Tick 重复提交。

### 认知中断与恢复

没有 LLM 调用不等于认知中断。只有认知连续性明确进入睡眠、失去意识或其他中断状态时，才停止形成普通可事后消费的观察。

中断期间仍可检测会影响身体模式或唤醒条件的刺激，但不会形成完整经历。恢复上下文只包含恢复时仍可感知的持续刺激、身体感觉和当前环境证据，不补发已经消失的刺激、完整空档历史或隐藏原因。

## 必须继承的既有基础设施决策

### 配置和规则的复用原则

所有子系统必须复用统一配置能力；只有能够确定性声明的逻辑才按需使用共享规则内核。后续子系统不得另建结构近似但不兼容的配置解析器或规则引擎。

共享基础设施只计算候选结果。世界、人物、身体、感知、认知、记忆和时间调度继续独占各自的领域语义、不变量、权威状态和提交权限。

### 单一运行配置版本

运行时只暴露一个运行配置版本。解析后的配置、规则及编译产物、领域扩展语义和确定性运算语义都由该版本唯一确定。

配置内容、规则或领域扩展语义发生变化时，统一提升运行配置版本。编译产物或扩展组件可以有内部身份用于缓存和诊断，但不能作为调用方独立选择的运行版本。

### 原子配置更新

候选配置或新规则的输入、完整解析、共享验证、领域验证、编译和生效属于一个原子操作：

* 任一步骤失败，返回诊断，当前有效配置完全不变。
* 全部成功，一次提交完整新配置并使其成为当前有效版本。
* 外部不能观察到部分验证、部分编译或半生效状态，因此不设计业务层回滚流程。
* 已经开始的单次求值使用开始时取得的配置快照完成；更新后开始的新求值使用新配置。
* 未结束过程不永久绑定旧规则。已经提交的过程参数和进度不被倒改，下一次规则判断使用当前有效配置。

### 确定性求值边界

规则求值器采用接近纯函数的模型：输入为当前运行配置、只读领域状态快照、状态版本、显式模拟时间和触发信息；输出为候选效果与追踪。

求值期间禁止 LLM、随机数、机器时间、隐藏状态以及文件、网络、数据库等 I/O。相同运行配置版本和相同输入必须得到相同结果及顺序。

该限制只针对求值器。配置加载、配置原子提交、编译缓存、追踪落盘和快照持久化可以由求值器之外的组件执行 I/O。

### 状态和持久化归属

各领域系统分别维护并持久化自己的权威状态：

* 世界系统：世界事实、关系、世界过程和客观事件。
* 人物系统：Identity、生命周期和角色关联。
* 身体系统：身体指标、能力、资源、动作和身体过程。
* 后续感知、认知、记忆和时间调度系统：各自领域状态。
* 统一基础设施：当前运行配置、编译产物、索引、配置更新结果和求值追踪。

统一基础设施不保存其他领域权威状态的第二份副本。整体保存协调器未来负责在一致边界收集各领域快照及当前运行配置版本，再交给持久化接口写入存储；其具体内部设计留给时间与整体调度子系统。

## 后续子系统必须继承的接入边界

* **认知系统：** 可以复用参与许可、持续决定条件和候选唤醒条件；LLM 推理、意图和理解不进入确定性规则内核。
* **认知调度：** 规则只能产生建议调度，不能直接调用 LLM；排队、去重、取消和过时结果处理由调度器负责。
* **记忆系统：** 可以复用编码资格、巩固条件、评分和衰减映射；需维护角色自己的主观人物档案及其不透明实体锚点关联，并只向感知返回识别所需的最小结果。记忆痕迹与主观重建仍由记忆系统负责。
* **时间与整体调度：** 独占模拟时钟、Tick 阶段和整体保存协调；规则只能读取显式传入的模拟时间。
* **配置生成 Skill：** 只能生成候选配置，必须通过共享验证、领域验证和原子配置更新。本阶段不创建该 Skill。

## 全局设计边界

后续设计继续遵守：

* 世界 Ground Truth、角色感知、认知理解和记忆相互分离。
* LLM 只产生候选意图、配置、规则或变化，不能直接修改权威状态。
* Tick 只推进已经建立的确定性变化，不在 Tick 内等待 LLM。
* 用户角色与 AI 角色遵守相同的身体和世界规则，控制来源不是可感知事实。
* 身体与认知独立运行；没有 LLM 调用不等于意识中断。
* 身体可以限制认知参与，但认知恢复信息必须经过感知过滤。
* 配置与运行状态严格分离，配置更新不能倒改已经形成的领域状态。

## 尚待详细设计

* 认知系统与认知调度。
* 记忆系统。
* 时间与整体调度。
* 配置生成 Skill；轮到该子系统时需同时交付可供 AI Agent 使用的实际 Skill。

下一轮具体子系统由用户确认，并继续保持一次只详细设计一个子系统。

## 后续工作方式

1. 先阅读 `Readme.md` 和本交接。
2. 根据本轮目标读取直接相邻的详细设计；涉及跨系统权威边界时再检查所有受影响文档。
3. 先用简单语言确认会改变产品边界的关键取舍，再写正式设计。
4. 保持世界观无关，不加入具体角色、地点、物品、剧情或平衡数值。
5. 不提前固定编程语言、存储技术、类结构或协议字段。
6. 新详细设计保存在 `docs` 并加入 `Readme.md` 索引。

## 工作区状态

当前存在未提交文档修改，新增的 `docs/perception-system.md` 尚未跟踪。继续工作时保留现有详细设计，不要覆盖、删除或回退用户已有修改。当前没有 Git 提交要求。
